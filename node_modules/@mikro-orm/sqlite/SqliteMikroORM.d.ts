import { MikroORM } from '@mikro-orm/core';
import type { Options, Configuration, IDatabaseDriver } from '@mikro-orm/core';
import { SqliteDriver } from './SqliteDriver';
/**
 * @inheritDoc
 */
export declare class SqliteMikroORM extends MikroORM<SqliteDriver> {
    private static DRIVER;
    /**
     * @inheritDoc
     */
    static init<D extends IDatabaseDriver = SqliteDriver>(options?: Options<D> | Configuration<D>, connect?: boolean): Promise<MikroORM<D>>;
}
export type SqliteOptions = Options<SqliteDriver>;
export declare function defineSqliteConfig(options: SqliteOptions): Options<SqliteDriver>;
